# The configured root owns isolated workspace-* children. The Client folds
# their Sessions into one contributed sidebar group without changing Host membership.
- insert:
    - id: temporary-workspace
      name: dsh-temporary-workspace
      config:
        root: !!js dshHomePath('temporary-workspaces')
        reservationRetentionMs: 3600000
